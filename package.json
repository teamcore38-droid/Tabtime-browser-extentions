{
  "name": "tabtime-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-extension.mjs"
  },
  "engines": { "node": ">=20" }
}
